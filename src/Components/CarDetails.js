import React from 'react';

const CarDetails = () => {
    return (
        <div>
            <h1>Car Details</h1>
            <button>Edit</button>
            <button>Delete</button>
        </div>
    );
};

export default CarDetails;